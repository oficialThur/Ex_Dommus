# Use root/example as user/password credentials

services:

  db:
    image: mysql_local

    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "root"
      MYSQL_DATABASE: "Arthur_database"
    ports:
      - "3303:3306"
    volumes:
      - /var/lib/mysql
